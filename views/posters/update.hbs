{{#extends "base"}}
  {{#block "content"}}
    <h1>Update Poster : {{poster.title}}</h1>
    <form method="POST">
      {{{form}}}
      <input
        type="submit"
        value="Update Product"
        class="btn btn-primary mt-3"
      />
    </form>

  {{/block}}
{{/extends}}